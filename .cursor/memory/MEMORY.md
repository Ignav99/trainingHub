# TrainingHub — Memory

## Last updated
2026-08-18 — Microciclo en curso tab + tareas pizarra manual-first

## Recent work
- Branch `cursor/microciclo-tareas-manual-ae84`:
  - **Microciclos**: API `estado` filter for en_curso/planificado/etc; reset page on filter; keep filter pills visible when switching back to en_curso (`setShowFilters(true)` on pill click).
  - **Tareas**: Pizarra táctica primary on detail empty state; AI as optional "Boceto"; list page button hierarchy (Nueva Tarea primary); TaskPickerDialog AI demoted; nueva-ai redirects to `?pizarra=1` after save.

## Deploy chain
cursor/* PR → CI → auto-merge main → Deploy → Render
